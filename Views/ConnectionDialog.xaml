<Window x:Class="RDPManager.Views.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加 RDP 连接" SizeToContent="Height" Width="480"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="Segoe UI" FontSize="13"
        Background="#F0F2F5">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="FieldLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,8,0,3"/>
        </Style>
        <Style TargetType="TextBox" x:Key="FieldInput">
            <Setter Property="Height" Value="34"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bg" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Background="#FFFFFF">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bg" Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="BorderBrush" Value="#B0B0B0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox" x:Key="FieldPassword">
            <Setter Property="Height" Value="34"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Bg" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Background="#FFFFFF">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bg" Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="BorderBrush" Value="#B0B0B0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="DialogButton">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DropShadowEffect x:Key="Shadow" BlurRadius="16" Opacity="0.1" ShadowDepth="4" Color="#000000"/>
    </Window.Resources>

    <Border Background="#FFFFFF" CornerRadius="8" Margin="12" Effect="{StaticResource Shadow}">
        <Grid Margin="24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="RDP 连接信息" FontSize="16" FontWeight="SemiBold" Foreground="#1A1A1A" Margin="0,0,0,4"/>
            <TextBlock Grid.Row="0" Text="填写远程桌面连接参数" FontSize="11" Foreground="#888888" Margin="0,22,0,0"/>

            <TextBlock Grid.Row="1" Text="服务器地址" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Row="1" x:Name="TbServer" Style="{StaticResource FieldInput}" Margin="0,24,0,0"/>

            <TextBlock Grid.Row="2" Text="用户名" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Row="2" x:Name="TbUsername" Style="{StaticResource FieldInput}"/>

            <TextBlock Grid.Row="3" Text="密码" Style="{StaticResource FieldLabel}"/>
            <PasswordBox Grid.Row="3" x:Name="PbPassword" Style="{StaticResource FieldPassword}"/>

            <TextBlock Grid.Row="4" Text="备注" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Row="4" x:Name="TbNotes" Style="{StaticResource FieldInput}" Height="60" AcceptsReturn="True" TextWrapping="Wrap" Padding="10,6"/>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="BtnOk" Content="保存" Style="{StaticResource DialogButton}" Background="#0078D4" Foreground="#FFFFFF" Click="BtnOk_Click" IsDefault="True"/>
                <Button Content="取消" Style="{StaticResource DialogButton}" Background="#E0E0E0" Foreground="#333333" Margin="10,0,0,0" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
